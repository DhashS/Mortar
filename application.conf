{
    app = {
        port = 9000
    },
    local = {
        uname = ${USER},
        recvPath = "arst",
        toShoot = ["1", "2"],
        sec = {
            luksCipher = "aes-plain64",
            luksHash = "sha512",
            leakSize = true,
            ssh = {
                pub = "/home/"${local.uname}"/.ssh/id_rsa.pub",
                priv = "/home/"${local.uname}"/.ssh/id_rsa"
          }
        }
    }
}